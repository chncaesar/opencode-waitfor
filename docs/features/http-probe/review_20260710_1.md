# Review 1: HTTP probe

Date: 20260710
Branch: feature/http-probe

## Summary

Implementation is clean, spec-compliant, and well-structured. The probe correctly handles status matching (number/array/default-2xx), json_match with dot-path resolution, abort signal propagation, and body truncation. Full test suite (5/5 + 14 legacy = 19/19) passes. Two minor test-coverage gaps noted — no functional defects found. **Total: 0 Critical, 0 Major, 2 Minor.**

## Issues

### Minor

- [m1] Missing test coverage for `expect.status` as `number[]` — the brief's `HttpExpect.status` is typed `number | number[]`, the implementation correctly handles arrays via `Array.isArray`, but no test exercises the array path (e.g. `{ status: [200, 302] }`). — `test/http.test.ts`

- [m2] No test verifying `bodyPreview` truncation to `maxBytes` — the spec requires body truncated to `opts.maxBytes`, and the implementation does `text.slice(0, opts.maxBytes)`, but no test validates that a response body longer than `maxBytes` is correctly truncated in the snapshot. — `test/http.test.ts`

## Coder Response

